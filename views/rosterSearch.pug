extends layout

block content
	div.container.text-center
		div.row

			h1= title
			h2= searchString

			if inClass
				div.col-sm-6.col-sm-offset-3
					h3 You are in the cohort!
			if !inClass
				div.col-sm-6.col-sm-offset-3
					h3 You are NOT in the cohort!
